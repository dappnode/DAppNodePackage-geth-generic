version: "3.5"
services:
  geth:
    build:
      context: geth
      args:
        UPSTREAM_VERSION: v1.14.0
        DATA_DIR: /root/.ethereum
    volumes:
      - geth:/root/.ethereum
    environment:
      EXTRA_OPTS: ""
      SYNCMODE: snap
    restart: unless-stopped
volumes:
  geth: {}
